#!groovy
// Check ub1 properties
properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage("First step") {
            steps {
                sh "ssh root@192.168.100.174 'hostname'"
            }
        }
        stage("Second step") {
            steps {
                sh 'ssh root@192.168.100.174 \'date\''
            }
        }
    }
}
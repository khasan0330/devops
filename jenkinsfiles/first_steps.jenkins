// мой первый пайплайн

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("First step") {
            steps {
                sh "ssh root@192.168.100.174 'hostname'"
            }
        }
        stage("Second step") {
            steps {
                sh "ssh root@192.168.100.174 'ip a'"
            }
        }
    }
}
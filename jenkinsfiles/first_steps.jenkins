// мой первый пайплайн

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {

        stage("Second step") {
            steps {
                sh "ssh root@prod1 'cd /var/github && git clone git@github.com:khasan0330/python.git'"
            }
        }
    }
}
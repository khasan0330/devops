// update

pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage("update bot") {
            steps {
                echo "==============Start update code================================"
                dir ('/var/github/python') {
                sh "ssh root@prod1 'git push'"
                }
            }
        }
    }
}
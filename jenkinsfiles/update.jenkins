// update

pipeline {
    agent any
    triggers {
        pollSCM('* * * * *')
     }
    options {
        timestamps()
    }
    stages {
        stage("update bot") {
            steps {
                echo "==============Start update code================================"

                sh "ssh root@prod1 'cd /var/github/python && git pull'"

            }
        }
    }
}